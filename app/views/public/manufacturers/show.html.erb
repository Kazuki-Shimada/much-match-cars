<h2 class="mx-5 mt-3"><%= @manufacturer.name %></h2>
<div class="container text-center">
  <table class="table">
    <% @manufacturer.models.each do |model| %>
      <tr>
        <th>
          <%= link_to model_path(model) do %>
            <%= model.name %>
          <% end %>
        </th>
      </tr>
    <% end %>
  </table>
</div>